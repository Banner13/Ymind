```
fastboot - run as a fastboot usb or udp device

Usage:
fastboot [-l addr] [-s size] usb <controller> | udp
        addr - address of buffer used during data transfers (0x83800000)
        size - size of buffer used during data transfers (0x40000000)
```